var func = function(source, target, context) {
  // console.log('111', JSON.stringify(source.event.detail))
  var detail = source.event.detail
  return {
    style: {left: (detail.x / 4) + 'px'}
  }
}
module.exports = {
  func: func
}